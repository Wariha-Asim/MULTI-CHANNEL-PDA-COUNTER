# MULTI-CHANNEL-PDA-COUNTER
A Tkinter app simulating 3 independent PDA counters (A, B, C). Increment pushes to stack, reset clears, and states move q0→q1→qf. Counts persist using JSON. Choose a channel, increment, reset, and get a beep when target is reached.
